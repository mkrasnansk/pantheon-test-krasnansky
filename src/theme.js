import { createTheme } from "@material-ui/core/styles";
export const theme = createTheme({
  palette: {
    primary: {
      main: "#ccc",
    },
    secondary: {
      main: "#333",
    },
    error: {
      main: "#ccc",
    },
    text: {
      secondary: "#272727",
      disabled: "#333",
    },
  },
});
